"use strict";
const $=id=>document.getElementById(id),KEY="organisateurV3",LEGACY=["coordoV2","coordoCleanV6"];
const COLORS=["#CFE2FF","#DCCBFF","#CDEFD8","#FFE2B8","#FFD5E5","#CBEDEA","#FFF0A8","#D7E3F4"];
const CATS=[{name:"Coordo",color:"#2f6af2"},{name:"Coordo Com",color:"#8a4de8"},{name:"Coordo PAPS",color:"#28b463"},{name:"Coordo Logistique",color:"#f2a514"},{name:"Coordo Bac",color:"#22b8b2"},{name:"Perso",color:"#64748b"}];
const HCOL=[{name:"BAC",color:"#CDECCF"},{name:"Parents-profs",color:"#FFF0A8"},{name:"Réunion coordo",color:"#CFE2FF"},{name:"Rattrapage",color:"#FFD6A0"},{name:"Formation",color:"#E5D5FF"},{name:"Important",color:"#FFD3DF"},{name:"Projet",color:"#CCEFED"},{name:"Administratif",color:"#E2E8F0"}];
const CONTACTS=[
["Jérôme iCloud","j.barletta@icloud.com","Monsieur"],["Jérôme Vauban","j.barletta@vauban.lu","Monsieur"],["M. Vecrigner","m.vecrigner@vauban.lu","Monsieur"],["C. Jeudy","c.jeudy@vauban.lu",""] ,["H. Lacouque","h.lacouque@vauban.lu","Madame"],["G. Mansouri","g.mansouri@vauban.lu","Madame"],["H. De Rosa","h.derosa@vauban.lu","Monsieur"],["P. Perfetto","p.perfetto@vauban.lu","Monsieur"],["G. Ludwig","g.ludwig@vauban.lu",""] ,["A. Le Pannerer","a.lepannerer@vauban.lu","Monsieur"],["N. La Rosa","n.larosa@vauban.lu","Madame"],["P. Tezza","p.tezza@vauban.lu","Monsieur"]];
const uid=()=>Date.now().toString(36)+Math.random().toString(36).slice(2,9),esc=s=>String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]));
const iso=(d=new Date())=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`,today=()=>iso(),plus=n=>{let d=new Date();d.setHours(12,0,0,0);d.setDate(d.getDate()+n);return iso(d)},date=s=>new Date(s+"T12:00:00"),fmt=s=>!s?"Sans date":new Intl.DateTimeFormat("fr-FR",{weekday:"short",day:"numeric",month:"short"}).format(date(s)),long=()=>new Intl.DateTimeFormat("fr-FR",{weekday:"long",day:"numeric",month:"long",year:"numeric"}).format(new Date());
const mins=v=>{let m=String(v||"").match(/^(\d{1,2}):(\d{2})/);return m?+m[1]*60 + +m[2]:null},done=t=>t.status==="Terminé",late=t=>!done(t)&&t.date&&t.date<today(),rank=p=>p==="urgent"?0:p==="haute"?1:p==="normale"?2:3,sort=(a,b)=>late(a)!==late(b)?(late(a)?-1:1):rank(a.priority)-rank(b.priority)||(a.date||"9999").localeCompare(b.date||"9999")||(a.startTime||"").localeCompare(b.startTime||"");
const base=()=>({version:3,tasks:[],categories:CATS.map(x=>({...x})),calendars:[],hiddenCalendarEvents:[],highlightColors:HCOL.map(x=>({...x})),eventHighlights:{},contacts:CONTACTS.map(x=>({id:uid(),name:x[0],email:x[1],civility:x[2]})),contactGroups:[],selectedContactIds:[],selectedGroupIds:[],mailMode:"pro",teamTone:"normal",agendaView:"list"});
function norm(o){let b=base(),s={...b,...(o||{})};s.tasks=(Array.isArray(s.tasks)?s.tasks:[]).map(t=>({id:t.id||uid(),title:t.title||"Sans titre",category:t.category||"Coordo",type:t.type||"Autre",status:t.status||"À faire",priority:t.priority||"normale",date:t.date||"",allDay:!!t.allDay,startTime:t.startTime||"",endTime:t.endTime||"",notes:t.notes||"",quick:!!(t.quick||t.inbox),reminder:t.reminder||"none",createdAt:t.createdAt||Date.now()}));s.categories=Array.isArray(s.categories)&&s.categories.length?s.categories:b.categories;s.calendars=Array.isArray(s.calendars)?s.calendars:[];s.hiddenCalendarEvents=Array.isArray(s.hiddenCalendarEvents)?s.hiddenCalendarEvents:[];s.highlightColors=Array.isArray(s.highlightColors)&&s.highlightColors.length?s.highlightColors:b.highlightColors;s.eventHighlights=s.eventHighlights||{};s.contacts=(Array.isArray(s.contacts)&&s.contacts.length?s.contacts:b.contacts).map(c=>({id:c.id||uid(),name:c.name||"",email:c.email||"",civility:c.civility||""}));s.contactGroups=(Array.isArray(s.contactGroups)?s.contactGroups:[]).map((g,i)=>({id:g.id||uid(),name:g.name||"Groupe",contactIds:Array.isArray(g.contactIds)?g.contactIds:[],archived:!!g.archived,color:g.color||COLORS[i%COLORS.length]}));s.selectedContactIds=Array.isArray(s.selectedContactIds)?s.selectedContactIds:[];s.selectedGroupIds=Array.isArray(s.selectedGroupIds)?s.selectedGroupIds:[];return s}
function load(){try{let x=localStorage.getItem(KEY);if(x)return norm(JSON.parse(x));for(let k of LEGACY){x=localStorage.getItem(k);if(x){let s=norm(JSON.parse(x));localStorage.setItem(KEY,JSON.stringify(s));return s}}}catch(e){}return base()}let state=load(),route="home",monthDate=new Date(),editing=null;const save=()=>localStorage.setItem(KEY,JSON.stringify(state)),cat=n=>state.categories.find(c=>c.name===n)||{color:"#64748b"};
const titles={home:["Hello",long()],tasks:["Tâches","Tout au même endroit"],agenda:["Agenda","Tâches + calendriers externes"],mail:["Mail","Idées → ChatGPT → Mail"],settings:["Réglages","Sauvegarde, calendriers et personnalisation"]};
function due(t){if(!t.date)return"Sans date";if(late(t))return`⚠ ${Math.max(1,Math.round((date(today())-date(t.date))/864e5))} j de retard`;if(t.date===today())return"Aujourd’hui";if(t.date===plus(1))return"Demain";return fmt(t.date)}
function icon(t){return({Mail:"✉",Appel:"☎",Réunion:"●",Document:"▤",Relance:"↻",Idée:"◇",Autre:"•"})[t.type||"Autre"]}
function key(i){return i.kind==="task"?`task|${i.task.id}`:[i.source||"",i.date||"",i.start||"",i.end||"",i.title||""].join("|")}
function items(hidden=false){let a=state.tasks.filter(t=>!done(t)&&t.date).map(t=>({kind:"task",date:t.date,start:t.startTime,end:t.endTime,title:t.title,source:t.category,color:cat(t.category).color,task:t}));for(let c of state.calendars){if(c.enabled===false)continue;for(let e of c.events||[])a.push({kind:"event",date:e.date,start:e.start||"",end:e.end||"",title:e.title||"Événement",source:c.name,color:c.color||"#64748b",location:e.location||""})}return a.filter(i=>i.date&&(hidden||!state.hiddenCalendarEvents.includes(key(i)))).sort((a,b)=>a.date.localeCompare(b.date)||(a.start||"").localeCompare(b.start||""))}
function conflicts(t,id=null){if(!t.date||t.allDay||!t.startTime||!t.endTime)return[];let s=mins(t.startTime),e=mins(t.endTime);if(e<=s)return[];return items().filter(i=>!(i.kind==="task"&&i.task.id===id)&&i.date===t.date&&i.start&&i.end&&s<mins(i.end)&&e>mins(i.start))}
function row(t){let c=cat(t.category),p=t.priority==="urgent"?"urgent":t.priority==="haute"?"high":t.priority==="basse"?"low":"normal";return `<div class="row" style="background:${state.eventHighlights[`task|${t.id}`]||"#fff"}"><input class="check" data-done="${t.id}" type="checkbox" ${done(t)?"checked":""}><div class="bar" style="background:${c.color}"></div><div class="rowMain" data-task="${t.id}"><div class="rowTitle">${icon(t)} ${esc(t.title)}</div><div class="rowMeta"><span class="pill white" style="background:${c.color}">${esc(t.category)}</span><span class="pill ${p}">${t.priority}</span>${t.reminder!=="none"?'<span class="pill info">Rappel</span>':""}<span class="when ${late(t)?"late":""}">${due(t)}${t.startTime?` · ${t.startTime}${t.endTime?`–${t.endTime}`:""}`:""}</span></div></div><div class="chev" data-task="${t.id}">›</div></div>`}
function section(n,sub,a,empty){return `<div class="section"><div class="sectionTitle"><div><h2>${n}</h2><div class="sub">${sub}</div></div></div><div class="card list">${a.length?a.map(row).join(""):`<div class="empty">${empty}</div>`}</div></div>`}
function home(){let a=state.tasks.filter(t=>!done(t)),td=a.filter(t=>t.date===today()).sort(sort),tm=a.filter(t=>t.date===plus(1)).sort(sort),now=a.filter(t=>t.date!==today()&&t.date!==plus(1)&&(late(t)||(!t.date&&["urgent","haute"].includes(t.priority)))).sort(sort).slice(0,6),used=new Set([...td,...tm,...now].map(t=>t.id)),follow=a.filter(t=>!used.has(t.id)&&(t.status==="En attente"||t.type==="Relance")).sort(sort);follow.forEach(t=>used.add(t.id));let later=a.filter(t=>!used.has(t.id)&&t.date&&t.date>plus(1)).sort(sort).slice(0,10);return `<div class="stats"><div class="stat"><b>${a.length}</b><span>À faire</span></div><div class="stat"><b>${a.filter(t=>t.priority==="urgent").length}</b><span>Urgentes</span></div><div class="stat"><b>${a.filter(late).length}</b><span>En retard</span></div><div class="stat"><b>${td.length}</b><span>Aujourd’hui</span></div></div>${section("Maintenant","Les tâches vraiment prioritaires",now,"Rien à traiter immédiatement.")}${section("Aujourd’hui","Ce qui est prévu aujourd’hui",td,"Rien de prévu aujourd’hui.")}${section("Demain","Ce qui arrive demain",tm,"Rien de prévu demain.")}${section("À relancer","Suivis et éléments en attente",follow,"Aucune relance en attente.")}${section("Plus tard","Le reste de tes tâches planifiées",later,"Rien de prévu plus tard.")}`}
function tasks(){return `<div class="toolbar"><select id="fc"><option>Toutes catégories</option>${state.categories.map(c=>`<option>${esc(c.name)}</option>`).join("")}</select><select id="fs"><option>À faire</option><option>Tous</option><option>En cours</option><option>En attente</option><option>Terminé</option></select><select id="fo"><option value="smart">Priorité intelligente</option><option value="date">Par date</option><option value="priority">Par priorité</option><option value="category">Par catégorie</option></select></div><div id="taskList" class="card list"></div><div class="section card pad"><div class="sectionTitle"><div><h2>Note rapide</h2><div class="sub">Une idée à classer plus tard</div></div></div><input id="quick" placeholder="Ex. Relancer Vincent"><div class="grid2" style="margin-top:8px"><select id="qp"><option value="normale">Normale</option><option value="haute">Haute</option><option value="urgent">Urgent</option></select><input id="qd" type="date"></div><button id="qa" class="btn primary full" style="margin-top:8px">Ajouter</button></div>`}
function drawTasks(){let a=[...state.tasks],c=$("fc").value,s=$("fs").value,o=$("fo").value;if(c!=="Toutes catégories")a=a.filter(t=>t.category===c);if(s!=="Tous")a=a.filter(t=>t.status===s);o==="smart"?a.sort(sort):o==="date"?a.sort((x,y)=>(x.date||"9999").localeCompare(y.date||"9999")):o==="priority"?a.sort((x,y)=>rank(x.priority)-rank(y.priority)):a.sort((x,y)=>x.category.localeCompare(y.category));$("taskList").innerHTML=a.length?a.map(row).join(""):'<div class="empty">Aucune tâche.</div>';bindRows($("taskList"))}
function arow(i){let h=state.eventHighlights[key(i)]||"";return `<div class="row" style="background:${h||"#fff"}"><div></div><div class="bar" style="background:${h||i.color}"></div><div class="rowMain" data-ekey="${esc(key(i))}"><div class="rowTitle">${esc(i.title)}</div><div class="rowMeta"><span class="when">${i.start?i.start+(i.end?`–${i.end}`:"")+" · ":""}${esc(i.source)}</span></div></div><div class="chev" data-ekey="${esc(key(i))}">›</div></div>`}
function month(){let y=monthDate.getFullYear(),m=monthDate.getMonth(),start=(new Date(y,m,1).getDay()+6)%7,n=new Date(y,m+1,0).getDate(),a=items(),h=`<div class="month"><div class="monthHead"><button class="btn" data-mm="-1">‹</button><strong>${new Intl.DateTimeFormat("fr-FR",{month:"long",year:"numeric"}).format(monthDate)}</strong><button class="btn" data-mm="1">›</button></div><div class="week"><div>Lun</div><div>Mar</div><div>Mer</div><div>Jeu</div><div>Ven</div><div>Sam</div><div>Dim</div></div><div class="monthGrid">`;for(let i=0;i<start;i++)h+='<div class="day blank"></div>';for(let d=1;d<=n;d++){let x=`${y}-${String(m+1).padStart(2,"0")}-${String(d).padStart(2,"0")}`,r=a.filter(i=>i.date===x);h+=`<div class="day ${x===today()?"today":""}" data-day="${x}"><div class="dayNum">${d}</div>${r.slice(0,4).map(i=>`<div class="monthEvent" style="background:${state.eventHighlights[key(i)]||i.color};color:${state.eventHighlights[key(i)]?"#142033":"#fff"}">${esc(i.title)}</div>`).join("")}</div>`}return h+"</div></div>"}
function agenda(){let controls=`<div class="viewSwitch"><button id="vl" class="btn ${state.agendaView==="list"?"active":""}">Liste</button><button id="vm" class="btn ${state.agendaView==="month"?"active":""}">Mois</button></div><div class="grid2"><button id="sync" class="btn soft">Synchroniser</button><button id="cal" class="btn soft">Calendriers</button></div>`;if(state.agendaView==="month")return controls+`<div class="section">${month()}</div>`;let a=items().filter(i=>i.date>=today()),g={};a.forEach(i=>(g[i.date]??=[]).push(i));return controls+(Object.entries(g).map(([d,r])=>`<div class="section"><div class="sectionTitle"><h2>${d===today()?"Aujourd’hui":d===plus(1)?"Demain":fmt(d)}</h2></div><div class="card list">${r.map(arow).join("")}</div></div>`).join("")||'<div class="section card empty">Aucun événement.</div>')}
function mail(){let gs=state.contactGroups.filter(g=>!g.archived),sel=gs.filter(g=>state.selectedGroupIds.includes(g.id));return `<div class="card pad"><div class="field"><label>Style du mail</label><div class="modeSwitch"><button id="pro" class="btn ${state.mailMode==="pro"?"active":""}">Professionnel</button><button id="team" class="btn ${state.mailMode==="team"?"active":""}">Équipe</button></div></div>${state.mailMode==="team"?`<div class="field"><label>Ton équipe</label><div class="modeSwitch"><button id="normal" class="btn ${state.teamTone==="normal"?"active":""}">Normal</button><button id="humor" class="btn ${state.teamTone==="humor"?"active":""}">Avec humour</button></div></div>`:""}<div class="field"><label>Groupe</label><div class="groupSelectWrap"><select id="gs"><option value="">Choisir…</option>${gs.map(g=>`<option value="${g.id}">${esc(g.name)}</option>`).join("")}</select><button id="gm" class="btn soft">Gérer</button></div><div class="chips">${sel.map(g=>`<span class="chip" style="background:${g.color}">${esc(g.name)}<button data-rg="${g.id}">×</button></span>`).join("")}</div></div><div class="field"><label>Contacts individuels</label><button id="pick" class="btn soft full">Sélectionner (${state.selectedContactIds.length})</button></div><div class="field"><label>Autre adresse</label><input id="extra" type="email" placeholder="prenom.nom@exemple.com"></div></div><div class="card pad section"><div class="field"><label>Mes idées en vrac</label><textarea id="ideas"></textarea></div><button id="chat" class="btn primary full">Copier le prompt + ouvrir ChatGPT</button><div class="field" style="margin-top:14px"><label>Objet</label><input id="subject"></div><div class="field"><label>Mail final</label><textarea id="body" placeholder="Colle ici la réponse de ChatGPT"></textarea></div><div class="grid2"><button id="paste" class="btn soft">Coller la réponse</button><button id="openmail" class="btn primary">Ouvrir Mail</button></div><div id="mi" class="muted" style="margin-top:8px"></div></div>`}
function settings(){return `<div class="card pad"><div class="sectionTitle"><div><h2>Sauvegarde</h2><div class="sub">Les URL privées des calendriers sont exclues</div></div></div><div class="grid2"><button id="backup" class="btn soft">Sauvegarder</button><button id="restore" class="btn soft">Restaurer</button></div></div><div class="card pad section"><div class="sectionTitle"><h2>Organisation</h2></div><div class="grid2"><button id="cats" class="btn soft">Catégories</button><button id="highs" class="btn soft">Couleurs agenda</button></div></div><div class="card pad section"><div class="sectionTitle"><h2>Contacts & groupes</h2></div><div class="grid2"><button id="contacts" class="btn soft">Contacts</button><button id="groups" class="btn soft">Groupes</button></div></div><div class="card pad section"><div class="sectionTitle"><h2>Calendriers</h2></div><button id="cals" class="btn soft full">Calendriers synchronisés</button><button id="hidden" class="btn soft full" style="margin-top:8px">Événements masqués (${state.hiddenCalendarEvents.length})</button></div><div class="dangerBox section"><h3>Réinitialisation totale</h3><p>Efface toutes les données locales de l’application sur cet appareil.</p><button id="reset" class="btn danger full">Tout remettre à zéro</button></div>`}
function render(){$("pageTitle").textContent=titles[route][0];$("pageSubtitle").textContent=route==="home"?long():titles[route][1];$("topAction").style.display=["home","tasks","agenda"].includes(route)?"block":"none";$("fab").style.display=["home","tasks","agenda"].includes(route)?"block":"none";$("view").innerHTML=route==="home"?home():route==="tasks"?tasks():route==="agenda"?agenda():route==="mail"?mail():settings();bind()}
function nav(r){route=r;document.querySelectorAll("[data-route]").forEach(b=>b.classList.toggle("active",b.dataset.route===r));render();scrollTo({top:0,behavior:"smooth"})}
function bindRows(root=document){root.querySelectorAll("[data-task]").forEach(x=>x.onclick=()=>detail(x.dataset.task));root.querySelectorAll("[data-done]").forEach(x=>x.onchange=()=>{let t=state.tasks.find(t=>t.id===x.dataset.done);if(t){t.status=x.checked?"Terminé":"À faire";save();render()}})}
function bind(){bindRows();if(route==="tasks"){["fc","fs","fo"].forEach(id=>$(id).onchange=drawTasks);drawTasks();$("qa").onclick=()=>{let title=$("quick").value.trim();if(!title)return;state.tasks.push({id:uid(),title,category:"Coordo",type:"Autre",status:"À faire",priority:$("qp").value,date:$("qd").value,startTime:"",endTime:"",notes:"",quick:true,reminder:"none",createdAt:Date.now()});save();render()}}if(route==="agenda")bindAgenda();if(route==="mail")bindMail();if(route==="settings")bindSettings()}
function modal(h){$("modalRoot").innerHTML=`<div class="modalbg"><div class="modal">${h}</div></div>`;document.querySelectorAll("[data-close]").forEach(x=>x.onclick=close);$("modalRoot").querySelector(".modalbg").onclick=e=>{if(e.target.classList.contains("modalbg"))close()}}
function close(){editing=null;$("modalRoot").innerHTML=""}function toast(s){$("toastRoot").innerHTML=`<div class="toast">${esc(s)}</div>`;setTimeout(()=>$("toastRoot").innerHTML="",2200)}
function taskForm(d="",t=null){editing=t?.id||null;t=t||{title:"",category:"Coordo",type:"Autre",status:"À faire",priority:"normale",date:d||today(),allDay:false,startTime:"",endTime:"",notes:"",reminder:"none"};modal(`<div class="modalHead"><h3>${editing?"Modifier":"Nouvelle tâche"}</h3><button class="close" data-close>×</button></div><div class="field"><label>Titre</label><input id="mt" value="${esc(t.title)}"></div><div class="formgrid"><div class="field"><label>Catégorie</label><select id="mc">${state.categories.map(c=>`<option ${c.name===t.category?"selected":""}>${esc(c.name)}</option>`).join("")}</select></div><div class="field"><label>Priorité</label><select id="mp">${["urgent","haute","normale","basse"].map(p=>`<option ${p===t.priority?"selected":""}>${p}</option>`).join("")}</select></div><div class="field"><label>Type</label><select id="mtype">${["Mail","Appel","Réunion","Document","Relance","Idée","Autre"].map(x=>`<option ${x===t.type?"selected":""}>${x}</option>`).join("")}</select></div><div class="field"><label>Statut</label><select id="mst">${["À faire","En cours","En attente","Terminé"].map(x=>`<option ${x===t.status?"selected":""}>${x}</option>`).join("")}</select></div><div class="field"><label>Date</label><input id="md" type="date" value="${t.date||""}"></div><div class="field"><label><input id="mall" type="checkbox" style="width:auto;min-height:0" ${t.allDay?"checked":""}> Toute la journée</label></div><div class="field"><label>Début</label><input id="ms" type="time" value="${t.startTime||""}"></div><div class="field"><label>Fin</label><input id="me" type="time" value="${t.endTime||""}"></div></div><div id="cf" class="conflict"></div><div class="field"><label>Rappel</label><select id="mr"><option value="none">Aucun</option><option value="10m">10 min avant</option><option value="1h">1 h avant</option><option value="2h">2 h avant</option><option value="1d">1 jour avant</option></select></div><div class="field"><label>Surlignage</label><select id="mh"><option value="">Aucun</option>${state.highlightColors.map(c=>`<option value="${c.color}" ${state.eventHighlights[`task|${t.id}`]===c.color?"selected":""}>${esc(c.name)}</option>`).join("")}</select></div><div class="field"><label>Notes</label><textarea id="mn">${esc(t.notes||"")}</textarea></div><div class="actions"><button class="btn" data-close>Annuler</button><button id="sav" class="btn primary">Enregistrer</button></div>`);$("mr").value=t.reminder||"none";let check=()=>{let all=$("mall").checked;$("ms").disabled=$("me").disabled=all;if(all){$("ms").value="";$("me").value=""}let box=$("cf"),x={date:$("md").value,allDay:all,startTime:$("ms").value,endTime:$("me").value},s=mins(x.startTime),e=mins(x.endTime);box.style.display="none";if(s!==null&&e!==null&&e<=s){box.style.display="block";box.textContent="L’heure de fin doit être après le début.";return}let c=conflicts(x,editing);if(c.length){box.style.display="block";box.innerHTML="Chevauchement avec :<br>"+c.map(i=>`• ${esc(i.title)} ${i.start||""}${i.end?`–${i.end}`:""}`).join("<br>")+"<br>Tu peux quand même enregistrer."}};$("mall").onchange=check;["md","ms","me"].forEach(id=>$(id).oninput=check);check();$("sav").onclick=()=>{let title=$("mt").value.trim(),s=mins($("ms").value),e=mins($("me").value);if(!title)return alert("Ajoute un titre.");if(!$("mall").checked&&s!==null&&e!==null&&e<=s)return alert("L’heure de fin doit être après le début.");let old=editing?state.tasks.find(x=>x.id===editing):null,n={id:editing||uid(),title,category:$("mc").value,type:$("mtype").value,status:$("mst").value,priority:$("mp").value,date:$("md").value,allDay:$("mall").checked,startTime:$("mall").checked?"":$("ms").value,endTime:$("mall").checked?"":$("me").value,notes:$("mn").value.trim(),quick:old?.quick||false,reminder:$("mr").value,createdAt:old?.createdAt||Date.now()};if(conflicts(n,editing).length&&!confirm("Ce créneau chevauche un événement. Enregistrer quand même ?"))return;if(editing)state.tasks=state.tasks.map(x=>x.id===editing?n:x);else state.tasks.push(n);let h=$("mh").value;if(h)state.eventHighlights[`task|${n.id}`]=h;else delete state.eventHighlights[`task|${n.id}`];save();close();render()}}
function detail(id){let t=state.tasks.find(x=>x.id===id);if(!t)return,c=cat(t.category);modal(`<div class="modalHead"><h3>Détail</h3><button class="close" data-close>×</button></div><div class="detailTitle">${icon(t)} ${esc(t.title)}</div><div class="rowMeta"><span class="pill white" style="background:${c.color}">${esc(t.category)}</span><span class="pill">${esc(t.status)}</span><span class="when">${due(t)}${t.startTime?` · ${t.startTime}${t.endTime?`–${t.endTime}`:""}`:""}</span></div>${t.notes?`<div class="detailNotes">${esc(t.notes).replace(/(https?:\/\/[^\s<]+)/g,'<a class="link" href="$1" target="_blank">$1</a>')}</div>`:""}<button id="ics" class="btn soft full" style="margin-top:12px">Ajouter au calendrier iPhone</button><div class="actions"><button id="edit" class="btn primary">Modifier</button><button id="post" class="btn purple">Reporter</button><button id="toggle" class="btn soft">${done(t)?"Réouvrir":"Terminer"}</button><button id="del" class="btn danger">Supprimer</button></div>`);$("edit").onclick=()=>{close();taskForm(t.date,t)};$("post").onclick=()=>{t.date=plus(1);save();close();render()};$("toggle").onclick=()=>{t.status=done(t)?"À faire":"Terminé";save();close();render()};$("del").onclick=()=>{if(confirm("Supprimer cette tâche ?")){state.tasks=state.tasks.filter(x=>x.id!==id);save();close();render()}};$("ics").onclick=()=>exportICS(t)}
function exportICS(t){if(!t.date)return alert("Ajoute d’abord une date.");let clean=s=>String(s||"").replace(/\\/g,"\\\\").replace(/\n/g,"\\n").replace(/,/g,"\\,").replace(/;/g,"\\;"),dt=(d,h="")=>d.replace(/-/g,"")+(h?`T${h.replace(":","")}00`:""),lines=["BEGIN:VCALENDAR","VERSION:2.0","PRODID:-//Organisateur//FR","BEGIN:VEVENT",`UID:${t.id}@organisateur`,`DTSTAMP:${new Date().toISOString().replace(/[-:]/g,"").replace(/\.\d{3}Z$/,"Z")}`];if(t.allDay||!t.startTime){lines.push(`DTSTART;VALUE=DATE:${dt(t.date)}`)}else{lines.push(`DTSTART:${dt(t.date,t.startTime)}`);if(t.endTime)lines.push(`DTEND:${dt(t.date,t.endTime)}`)}lines.push(`SUMMARY:${clean(t.title)}`);if(t.notes)lines.push(`DESCRIPTION:${clean(t.notes)}`);let trig={"10m":"-PT10M","1h":"-PT1H","2h":"-PT2H","1d":"-P1D"}[t.reminder];if(trig)lines.push("BEGIN:VALARM",`TRIGGER:${trig}`,"ACTION:DISPLAY",`DESCRIPTION:${clean(t.title)}`,"END:VALARM");lines.push("END:VEVENT","END:VCALENDAR");let b=new Blob([lines.join("\r\n")],{type:"text/calendar"}),u=URL.createObjectURL(b),a=document.createElement("a");a.href=u;a.download="organisateur.ics";a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)}
function bindAgenda(){$("vl").onclick=()=>{state.agendaView="list";save();render()};$("vm").onclick=()=>{state.agendaView="month";save();render()};$("sync").onclick=sync;$('cal').onclick=calSettings;document.querySelectorAll("[data-ekey]").forEach(x=>x.onclick=()=>eventDetail(x.dataset.ekey));document.querySelectorAll("[data-mm]").forEach(x=>x.onclick=()=>{monthDate=new Date(monthDate.getFullYear(),monthDate.getMonth()+ +x.dataset.mm,1);render()});document.querySelectorAll("[data-day]").forEach(x=>x.onclick=()=>dayDetail(x.dataset.day))}
async function sync(){let ok=0,fail=0;for(let c of state.calendars){if(c.enabled===false)continue;try{let r=await fetch("/api/calendar",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({url:c.url})});if(!r.ok)throw 0;let d=await r.json();c.events=d.events||[];c.lastSync=new Date().toISOString();ok++}catch(e){fail++}}save();toast(fail?`${ok} OK, ${fail} en erreur`:`${ok} calendrier(s) à jour`);render()}
function eventDetail(k){let i=items(true).find(x=>key(x)===k);if(!i)return;if(i.kind==="task")return detail(i.task.id);modal(`<div class="modalHead"><h3>Événement</h3><button class="close" data-close>×</button></div><div class="detailTitle">${esc(i.title)}</div><div class="rowMeta"><span class="pill white" style="background:${i.color}">${esc(i.source)}</span><span class="when">${fmt(i.date)}${i.start?` · ${i.start}${i.end?`–${i.end}`:""}`:""}</span></div>${i.location?`<div class="card pad section">${esc(i.location)}</div>`:""}<button id="hilite" class="btn soft full" style="margin-top:12px">Surligner / changer la couleur</button><button id="hide" class="btn soft full" style="margin-top:8px">Masquer dans l’application</button>`);$("hilite").onclick=()=>palette(k);$("hide").onclick=()=>{if(confirm("Masquer cet événement uniquement dans l’application ?")){if(!state.hiddenCalendarEvents.includes(k))state.hiddenCalendarEvents.push(k);save();close();render()}}}
function palette(k){modal(`<div class="modalHead"><h3>Surligner</h3><button class="close" data-close>×</button></div><div class="grid2">${state.highlightColors.map(c=>`<button class="btn" data-h="${c.color}" style="background:${c.color}">${esc(c.name)}</button>`).join("")}</div><button id="noh" class="btn full" style="margin-top:10px">Aucun</button>`);document.querySelectorAll("[data-h]").forEach(b=>b.onclick=()=>{state.eventHighlights[k]=b.dataset.h;save();close();render()});$("noh").onclick=()=>{delete state.eventHighlights[k];save();close();render()}}
function dayDetail(d){let a=items().filter(i=>i.date===d);modal(`<div class="modalHead"><h3>${fmt(d)}</h3><button class="close" data-close>×</button></div><div class="card list">${a.length?a.map(arow).join(""):'<div class="empty">Aucun événement.</div>'}</div><button id="dayadd" class="btn primary full" style="margin-top:10px">+ Ajouter une tâche</button>`);document.querySelectorAll("[data-ekey]").forEach(x=>x.onclick=()=>eventDetail(x.dataset.ekey));$("dayadd").onclick=()=>{close();taskForm(d)}}
function calSettings(){modal(`<div class="modalHead"><h3>Calendriers</h3><button class="close" data-close>×</button></div>${state.calendars.map((c,i)=>`<div class="source"><div class="dot" style="background:${c.color}"></div><div><div class="sourceName">${esc(c.name)}</div><div class="sourceMeta">${c.enabled===false?"Masqué":"Affiché"}</div></div><div class="sourceBtns"><button data-tc="${i}">${c.enabled===false?"Afficher":"Masquer"}</button><button data-ec="${i}">Modifier</button><button data-dc="${i}">Supprimer</button></div></div>`).join("")||'<div class="empty">Aucun calendrier.</div>'}<button id="ac" class="btn primary full">+ Ajouter</button>`);$("ac").onclick=()=>calEdit();document.querySelectorAll("[data-tc]").forEach(b=>b.onclick=()=>{let c=state.calendars[+b.dataset.tc];c.enabled=c.enabled===false;save();calSettings()});document.querySelectorAll("[data-ec]").forEach(b=>b.onclick=()=>calEdit(+b.dataset.ec));document.querySelectorAll("[data-dc]").forEach(b=>b.onclick=()=>{if(confirm("Supprimer ce calendrier ?")){state.calendars.splice(+b.dataset.dc,1);save();calSettings()}})}
function calEdit(i=null){let c=i===null?{name:"",url:"",color:"#2f6af2"}:state.calendars[i];modal(`<div class="modalHead"><h3>Calendrier</h3><button class="close" data-close>×</button></div><div class="field"><label>Nom</label><input id="cn" value="${esc(c.name)}"></div><div class="field"><label>URL ICS</label><input id="cu" value="${esc(c.url||"")}"></div><div class="field"><label>Couleur</label><input id="cc" type="color" value="${c.color||"#2f6af2"}"></div><div class="muted">L’URL reste locale et n’est pas exportée dans les sauvegardes.</div><button id="cs" class="btn primary full" style="margin-top:10px">Enregistrer</button>`);$("cs").onclick=()=>{let name=$("cn").value.trim(),url=$("cu").value.trim().replace(/^webcal:/i,"https:");if(!name||!/^https:\/\//i.test(url))return alert("Nom et URL HTTPS requis.");let x=i===null?{name,url,color:$("cc").value,enabled:true,events:[]}:{...c,name,url,color:$("cc").value};i===null?state.calendars.push(x):state.calendars[i]=x;save();calSettings()}}
function recipients(){return state.contacts.filter(c=>state.selectedContactIds.includes(c.id))}function prompt(ideas){let r=recipients().map(c=>`${c.civility?c.civility+" ":""}${c.name} <${c.email}>`).join(", ")||"Destinataire non identifié";if(state.mailMode==="pro")return `Rédige un mail professionnel en français à partir de mes idées en vrac.\nTon professionnel, naturel, fluide, précis et cordial, jamais robotique. Aucun emoji ou émoticône. Corrige les fautes et propose un objet court.\nDestinataire(s) : ${r}\nPour l’appel, utilise Bonjour Madame [Nom] ou Bonjour Monsieur [Nom] si la civilité est connue ; sinon reste neutre sans inventer.\nTermine exactement par :\nCordialement,\nJérôme BARLETTA\n\nMes idées :\n${ideas}\n\nRéponds STRICTEMENT :\nOBJET: [objet]\n---\n[mail prêt à envoyer]\nAucun commentaire.`;return `Rédige un mail à mon équipe en français. Ton naturel, direct et chaleureux.${state.teamTone==="humor"?" Ajoute une touche d’humour légère.":""} Aucun emoji ou émoticône. Commence par Hello,. Propose un objet court.\nDestinataire(s) : ${r}\nTermine exactement par :\nA +\nJérôme\n\nMes idées :\n${ideas}\n\nRéponds STRICTEMENT :\nOBJET: [objet]\n---\n[mail prêt à envoyer]\nAucun commentaire.`}
function parseMail(x){x=String(x||"").replace(/\r\n/g,"\n").trim();let lines=x.split("\n"),subject="",idx=-1;for(let i=0;i<Math.min(8,lines.length);i++){let c=lines[i].replace(/^\s*#{1,6}\s*/,"").replace(/\*\*/g,"").trim(),m=c.match(/^OBJET\s*:\s*(.+)$/i);if(m){subject=m[1].trim();idx=i;break}}let body=x;if(idx>=0){let r=lines.slice(idx+1);while(r.length&&(!r[0].trim()||/^[-–—]{3,}$/.test(r[0].trim())))r.shift();body=r.join("\n").trim()}let strip=s=>String(s).replace(/[\p{Extended_Pictographic}\p{Emoji_Presentation}\uFE0F\u200D]/gu,"").trim();return{subject:strip(subject),body:strip(body).replace(/(^|\n)\s*A\s*\+\s*[\/\\|]+/gi,"$1A +")}}
function applyMail(x){let p=parseMail(x);if(p.subject)$("subject").value=p.subject;$("body").value=p.body;$("mi").textContent=p.subject?"Objet et corps récupérés automatiquement.":"Objet non détecté automatiquement."}
function bindMail(){$("pro").onclick=()=>{state.mailMode="pro";save();render()};$("team").onclick=()=>{state.mailMode="team";save();render()};if($("normal"))$("normal").onclick=()=>{state.teamTone="normal";save();render()};if($("humor"))$("humor").onclick=()=>{state.teamTone="humor";save();render()};$("gs").onchange=()=>{let id=$("gs").value,g=state.contactGroups.find(x=>x.id===id);if(!g)return;if(!state.selectedGroupIds.includes(id))state.selectedGroupIds.push(id);g.contactIds.forEach(cid=>{if(!state.selectedContactIds.includes(cid))state.selectedContactIds.push(cid)});save();render()};document.querySelectorAll("[data-rg]").forEach(b=>b.onclick=()=>{state.selectedGroupIds=state.selectedGroupIds.filter(x=>x!==b.dataset.rg);save();render()});$("pick").onclick=contactPick;$("gm").onclick=groups;$("chat").onclick=async()=>{let ideas=$("ideas").value.trim();if(!ideas)return alert("Écris tes idées.");try{await navigator.clipboard.writeText(prompt(ideas));$("mi").textContent="Prompt copié."}catch(e){}window.open("https://chatgpt.com/","_blank")};$("paste").onclick=async()=>{try{applyMail(await navigator.clipboard.readText())}catch(e){$("mi").textContent="Appui long puis Coller dans Mail final."}};$("body").onpaste=e=>{let x=e.clipboardData?.getData("text");if(x){e.preventDefault();applyMail(x)}};$("openmail").onclick=()=>{let to=[...new Set(recipients().map(c=>c.email))],ex=$("extra").value.trim();if(ex)to.push(ex);if(!to.length)return alert("Choisis un destinataire.");let s=$("subject").value.trim(),b=$("body").value.trim();if(!s||!b)return alert("Objet ou corps manquant.");location.href=`mailto:${to.join(",")}?subject=${encodeURIComponent(s)}&body=${encodeURIComponent(b)}`}}
function contactPick(){modal(`<div class="modalHead"><h3>Contacts</h3><button class="close" data-close>×</button></div><input id="search" placeholder="Rechercher…"><div id="plist" class="contactSheetList" style="margin-top:8px"></div><button id="pd" class="btn primary full" style="margin-top:10px">Terminé</button>`);let draw=()=>{let q=$("search").value.toLowerCase();$("plist").innerHTML=state.contacts.filter(c=>!q||c.name.toLowerCase().includes(q)||c.email.toLowerCase().includes(q)).map(c=>`<label class="contactLine"><input type="checkbox" data-pc="${c.id}" ${state.selectedContactIds.includes(c.id)?"checked":""}><span><div class="contactName">${esc(c.name)}</div><div class="contactEmail">${esc(c.email)}</div></span></label>`).join("");document.querySelectorAll("[data-pc]").forEach(x=>x.onchange=()=>{x.checked?(!state.selectedContactIds.includes(x.dataset.pc)&&state.selectedContactIds.push(x.dataset.pc)):state.selectedContactIds=state.selectedContactIds.filter(i=>i!==x.dataset.pc);save()})};$("search").oninput=draw;draw();$("pd").onclick=()=>{close();render()}}
function contacts(){modal(`<div class="modalHead"><h3>Contacts</h3><button class="close" data-close>×</button></div>${state.contacts.map((c,i)=>`<div class="source"><div></div><div><div class="sourceName">${esc(c.civility?c.civility+" ":"")}${esc(c.name)}</div><div class="sourceMeta">${esc(c.email)}</div></div><div class="sourceBtns"><button data-ce="${i}">Modifier</button><button data-cd="${i}">Supprimer</button></div></div>`).join("")}<button id="ca" class="btn primary full">+ Ajouter</button>`);$("ca").onclick=()=>contactEdit();document.querySelectorAll("[data-ce]").forEach(b=>b.onclick=()=>contactEdit(+b.dataset.ce));document.querySelectorAll("[data-cd]").forEach(b=>b.onclick=()=>{let c=state.contacts[+b.dataset.cd];if(confirm(`Supprimer ${c.name} ?`)){state.contacts.splice(+b.dataset.cd,1);state.contactGroups.forEach(g=>g.contactIds=g.contactIds.filter(x=>x!==c.id));save();contacts()}})}
function contactEdit(i=null){let c=i===null?{name:"",email:"",civility:""}:state.contacts[i];modal(`<div class="modalHead"><h3>Contact</h3><button class="close" data-close>×</button></div><div class="field"><label>Civilité</label><select id="cv"><option value="">—</option><option ${c.civility==="Madame"?"selected":""}>Madame</option><option ${c.civility==="Monsieur"?"selected":""}>Monsieur</option></select></div><div class="field"><label>Nom</label><input id="cname" value="${esc(c.name)}"></div><div class="field"><label>Email</label><input id="cemail" value="${esc(c.email)}"></div><button id="csave" class="btn primary full">Enregistrer</button>`);$("csave").onclick=()=>{let name=$("cname").value.trim(),email=$("cemail").value.trim();if(!name||!email.includes("@"))return alert("Nom et email requis.");let x=i===null?{id:uid(),name,email,civility:$("cv").value}:{...c,name,email,civility:$("cv").value};i===null?state.contacts.push(x):state.contacts[i]=x;save();contacts()}}
function groups(){modal(`<div class="modalHead"><h3>Groupes</h3><button class="close" data-close>×</button></div>${state.contactGroups.map((g,i)=>`<div class="source"><div class="dot" style="background:${g.color}"></div><div><div class="sourceName">${esc(g.name)}</div><div class="sourceMeta">${g.contactIds.length} contact(s)${g.archived?" · archivé":""}</div></div><div class="sourceBtns"><button data-ge="${i}">Modifier</button><button data-ga="${i}">${g.archived?"Réactiver":"Archiver"}</button><button data-gd="${i}">Supprimer</button></div></div>`).join("")||'<div class="empty">Aucun groupe.</div>'}<button id="gadd" class="btn primary full">+ Créer</button>`);$("gadd").onclick=()=>groupEdit();document.querySelectorAll("[data-ge]").forEach(b=>b.onclick=()=>groupEdit(+b.dataset.ge));document.querySelectorAll("[data-ga]").forEach(b=>b.onclick=()=>{state.contactGroups[+b.dataset.ga].archived=!state.contactGroups[+b.dataset.ga].archived;save();groups()});document.querySelectorAll("[data-gd]").forEach(b=>b.onclick=()=>{if(confirm("Supprimer ce groupe ?")){state.contactGroups.splice(+b.dataset.gd,1);save();groups()}})}
function groupEdit(i=null){let g=i===null?{name:"",color:COLORS[state.contactGroups.length%COLORS.length],contactIds:[]}:state.contactGroups[i];modal(`<div class="modalHead"><h3>Groupe</h3><button class="close" data-close>×</button></div><div class="field"><label>Nom</label><input id="gn" value="${esc(g.name)}"></div><div class="field"><label>Couleur</label><input id="gc" type="color" value="${g.color}"></div><div class="contactSheetList">${state.contacts.map(c=>`<label class="contactLine"><input type="checkbox" data-gc="${c.id}" ${g.contactIds.includes(c.id)?"checked":""}><span><div class="contactName">${esc(c.name)}</div><div class="contactEmail">${esc(c.email)}</div></span></label>`).join("")}</div><button id="gsv" class="btn primary full" style="margin-top:10px">Enregistrer</button>`);$("gsv").onclick=()=>{let name=$("gn").value.trim(),ids=[...document.querySelectorAll("[data-gc]:checked")].map(x=>x.dataset.gc);if(!name||!ids.length)return alert("Nom et au moins un membre requis.");let x=i===null?{id:uid(),name,color:$("gc").value,contactIds:ids,archived:false}:{...g,name,color:$("gc").value,contactIds:ids};i===null?state.contactGroups.push(x):state.contactGroups[i]=x;save();groups()}}
function cats(){modal(`<div class="modalHead"><h3>Catégories</h3><button class="close" data-close>×</button></div>${state.categories.map((c,i)=>`<div class="source"><div class="dot" style="background:${c.color}"></div><input data-cn="${i}" value="${esc(c.name)}"><input data-cc="${i}" type="color" value="${c.color}" style="width:52px"><button class="btn" data-cx="${i}">×</button></div>`).join("")}<button id="cadd" class="btn primary full">+ Ajouter</button>`);document.querySelectorAll("[data-cn]").forEach(x=>x.onchange=()=>{let i=+x.dataset.cn,old=state.categories[i].name,n=x.value.trim();if(!n)return;state.categories[i].name=n;state.tasks.forEach(t=>{if(t.category===old)t.category=n});save()});document.querySelectorAll("[data-cc]").forEach(x=>x.onchange=()=>{state.categories[+x.dataset.cc].color=x.value;save()});document.querySelectorAll("[data-cx]").forEach(b=>b.onclick=()=>{if(state.categories.length<=1)return;let i=+b.dataset.cx,old=state.categories[i].name,rep=state.categories.find((_,j)=>j!==i).name;if(state.tasks.some(t=>t.category===old)&&!confirm(`Déplacer les tâches vers ${rep} ?`))return;state.tasks.forEach(t=>{if(t.category===old)t.category=rep});state.categories.splice(i,1);save();cats()});$("cadd").onclick=()=>{state.categories.push({name:"Nouvelle catégorie",color:"#2f6af2"});save();cats()}}
function highs(){modal(`<div class="modalHead"><h3>Couleurs agenda</h3><button class="close" data-close>×</button></div>${state.highlightColors.map((c,i)=>`<div class="source"><div class="dot" style="background:${c.color}"></div><input data-hn="${i}" value="${esc(c.name)}"><input data-hc="${i}" type="color" value="${c.color}" style="width:52px"><button class="btn" data-hx="${i}">×</button></div>`).join("")}<button id="ha" class="btn primary full">+ Ajouter</button>`);document.querySelectorAll("[data-hn]").forEach(x=>x.onchange=()=>{state.highlightColors[+x.dataset.hn].name=x.value.trim()||"Couleur";save()});document.querySelectorAll("[data-hc]").forEach(x=>x.onchange=()=>{let i=+x.dataset.hc,old=state.highlightColors[i].color,n=x.value;state.highlightColors[i].color=n;Object.keys(state.eventHighlights).forEach(k=>{if(state.eventHighlights[k]===old)state.eventHighlights[k]=n});save()});document.querySelectorAll("[data-hx]").forEach(b=>b.onclick=()=>{state.highlightColors.splice(+b.dataset.hx,1);save();highs()});$("ha").onclick=()=>{state.highlightColors.push({name:"Couleur",color:"#FFD6A0"});save();highs()}}
function hidden(){let a=items(true).filter(i=>i.kind==="event"&&state.hiddenCalendarEvents.includes(key(i)));modal(`<div class="modalHead"><h3>Événements masqués</h3><button class="close" data-close>×</button></div>${a.map(i=>`<div class="source"><div class="dot" style="background:${i.color}"></div><div><div class="sourceName">${esc(i.title)}</div><div class="sourceMeta">${fmt(i.date)}</div></div><button class="btn" data-show="${esc(key(i))}">Réafficher</button></div>`).join("")||'<div class="empty">Aucun événement masqué.</div>'}`);document.querySelectorAll("[data-show]").forEach(b=>b.onclick=()=>{state.hiddenCalendarEvents=state.hiddenCalendarEvents.filter(x=>x!==b.dataset.show);save();hidden()})}
function backup(){let safe={version:3,exportedAt:new Date().toISOString(),tasks:state.tasks,categories:state.categories,highlightColors:state.highlightColors,eventHighlights:state.eventHighlights,hiddenCalendarEvents:state.hiddenCalendarEvents,contacts:state.contacts,contactGroups:state.contactGroups,mailMode:state.mailMode,teamTone:state.teamTone,calendars:state.calendars.map(c=>({name:c.name,color:c.color,enabled:c.enabled!==false,url:null}))},b=new Blob([JSON.stringify(safe,null,2)],{type:"application/json"}),u=URL.createObjectURL(b),a=document.createElement("a");a.href=u;a.download=`organisateur-${today()}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)}
function bindSettings(){$("backup").onclick=backup;$("restore").onclick=()=>$("restoreFile").click();$("cats").onclick=cats;$("highs").onclick=highs;$("contacts").onclick=contacts;$("groups").onclick=groups;$("cals").onclick=calSettings;$("hidden").onclick=hidden;$("reset").onclick=()=>{modal(`<div class="modalHead"><h3>Réinitialisation totale</h3><button class="close" data-close>×</button></div><div class="dangerBox"><p>Tape RESET pour confirmer.</p><input id="rw"><button id="rr" class="btn danger full" style="margin-top:8px">Confirmer</button></div>`);$("rr").onclick=()=>{if($("rw").value.trim().toUpperCase()!=="RESET")return alert("Tape RESET.");if(confirm("Dernière confirmation ?")){localStorage.removeItem(KEY);LEGACY.forEach(k=>localStorage.removeItem(k));location.reload()}}}}
$("restoreFile").onchange=e=>{let f=e.target.files[0];if(!f)return,r=new FileReader();r.onload=()=>{try{let d=JSON.parse(r.result),urls=new Map(state.calendars.map(c=>[c.name,c.url]));state=norm({...state,...d,calendars:Array.isArray(d.calendars)?d.calendars.map(c=>({...c,url:urls.get(c.name)||"",events:[]})):state.calendars});save();alert("Sauvegarde restaurée. Les URL privées restent locales.");render()}catch(x){alert("Sauvegarde invalide.")}};r.readAsText(f);e.target.value=""};
document.querySelectorAll("[data-route]").forEach(b=>b.onclick=()=>nav(b.dataset.route));$("topAction").onclick=$("fab").onclick=()=>taskForm();nav("home");
